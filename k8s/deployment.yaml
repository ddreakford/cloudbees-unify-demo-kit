apiVersion: apps/v1
kind: Deployment
metadata:
  name: carechart-api
  labels:
    app: carechart-api
    cloudbees.demo/application: customer-portal
spec:
  replicas: 2
  selector:
    matchLabels:
      app: carechart-api
  template:
    metadata:
      labels:
        app: carechart-api
    spec:
      containers:
        - name: carechart-api
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 8080
          env:
            - name: APP_ENV
              value: demo
            - name: CUSTOMER_SCENARIO
              value: candidate-demo
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
