apiVersion: v1
kind: Service
metadata:
  name: carechart-api
  labels:
    app: carechart-api
spec:
  type: ClusterIP
  selector:
    app: carechart-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
